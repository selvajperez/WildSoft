# ui (pendiente)

Todavía no implementado. Queda para una sesión posterior, una vez validado
el catálogo scrapeado y una vez que exista `comparator/`.

La idea es una app Streamlit (como `alibaba-ml-comparador/app.py`) para
explorar el catálogo recolectado y, más adelante, el resultado del
comparador con Mercado Libre.
